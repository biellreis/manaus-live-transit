{
  "name": "manaus-live-transit",
  "version": "1.0.0",
  "description": "Manaus Live Transit — Web-Native Ultra PWA for Real-Time Public Transit",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "npm run dev:server & npm run dev:client",
    "build": "npm --prefix server run build && npm --prefix client run build && mkdir -p ios/ManausLiveTransit/WebDist && cp -r client/dist/* ios/ManausLiveTransit/WebDist/",
    "start": "npm --prefix server start"
  }
}
